# Copiar este archivo como .env y completar los valores reales.
# El .env real está en .gitignore: los secretos nunca se suben al repo.
SECRET_KEY=cambiame-por-algo-largo-y-aleatorio
# Opcional: si no se define, usa SQLite local (stockbox.db)
# DATABASE_URL=postgresql://usuario:password@host:5432/stockbox
